management:
  endpoints:
    jmx:
      exposure:
        include:
          refresh
spring:  
  application:
    name: springcloudconfigclient
  cloud:
    config:
      uri: http://localhost:4000
      fail-fast: true